<html>

<head>
  <title>Details</title>
  <style type="text/css">
    body {}
  </style>
</head>

<body> 

  <strong>Workout</strong>

  <p>{{ workout.date }}</p>

  <p>{{ workout.notes }}</p>

  <strong>Exercise List</strong>

  {% if exercise_list %}
    <ul id="recent">
    {% for exercise in exercise_list_out %}
      <li>
	<p><strong>{{ exercise.type.type }}</strong></p>
	{% if exercise.time %}
	  <p>Time: {{ exercise.time }} min</p>
	{% endif %}
	{% if exercise.dist %}
	  <p>Distance: {{ exercise.dist }} {{ exercise.units }}</p>
	{% endif %}
	{% if exercise.reps %}
	  <p>Reps: {{ exercise.reps }} reps</p>
	{% endif %}
	{% if exercise.weight %}
	  <p>Weight: {{ exercise.weight }} {{ exercise.units }}</p>
	{% endif %}
      </li>
      <hr noshade/>
    {% endfor %}
    </ul>
  {% else %}
    <p>No exercises are associated with this workout.</p>
  {% endif %}

</body>

</html>  
