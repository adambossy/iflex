<html>

<head>
  <title>Recent Workouts</title>
</head>

<body>

Welcome, {{ member.firstName }} {{ member.lastName }} (id={{ member.id }}).

  {% if recent_workouts %}
    <ul>
    {% for workout in recent_workouts %}
      <li>{{ workout.date }} {{ workout.notes }}</li>
    {% endfor %}
    </ul>
  {% else %}
    <p>No recent workouts are available.</p>
  {% endif %}

</body>

</html>
